build_settings:
    clone_depth: 1 
# depth of 1 is a shallow clone, remove this line to clone entire repo
    ignore:
        - "tests"
    path: 
        - "src"
test:
#    php_mess_detector:
#        allow_failures: true
#        rules:
#            - "cleancode"
#            - "controversial"
#            - "codesize"
#            - "design"
#            - "naming"
#            - "unusedcode"
    php_code_sniffer:
        allowed_warnings: true
        suffixes : "php"
        tab_width: 4
        allowed_errors: true 
        standard: "PSR2"
        encoding: "utf8"
        path: "src/JsonRPC"
    php_cpd:
        allow_failures: true
    php_loc:
    lint:
    php_unit:
        config: "phpunit.xml"
        path: "tests"